# Synthetic 2026 specification snapshot

This directory contains original, synthetic control definitions for the QI Sentinel proof of concept. It does not reproduce or summarize licensed HEDIS or CMS specification text.

`cbp.yml` defines the synthetic measure semantics exercised by `QI-SEM-001`. `col.yml` declares the lineage requirement exercised by `QI-LIN-001`. `value_sets.json` is the approved version registry for the snapshot.
