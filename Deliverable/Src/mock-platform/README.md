# Mock platform — seeded state

Every artifact in this directory is synthetic. The directory intentionally represents the four defects cataloged in `tests/fixtures/seeds.json`; it is not a clean or production-ready configuration.
